#ifndef CHANNEL_HPP
#define CHANNEL_HPP

#include <string>
#include <map>

using namespace std;

class Channel
{
public:
string type = "BSC";
int N = 0;
int seed = 0;

explicit Channel(const string &p = Cha)

};

#endif